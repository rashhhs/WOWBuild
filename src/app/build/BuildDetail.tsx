import React from 'react'
import { View } from 'react-native'

import Layout from '../../components/Layout'
import { apply, C } from '../../utils/styles'

const BuildDetail = () => {
  return (
    <Layout title={'Name Build'} insets={{ bottom: 0, top: 0 }}>
      {/*  */}
      <View style={apply(C.flex, C.bgRed)}>
        {/*<View style={C.row}>
          <Text
            variant={TextVariant.S}
            weight={FontWeight.Medium}
            style={apply(C.font5)}>
            {'Class :'}
          </Text>
          <Text variant={TextVariant.S} weight={FontWeight.Regular}>
            {'Death Knight'}
          </Text>
        </View>

        <View style={C.row}>
          <Text>{'Build :'}</Text>
          <Text>{'XX'}</Text>
        </View>

        <Text>{'Stats Prio:'}</Text>
        <Text>{'XXXX'}</Text>
        <Text>{'XXXX'}</Text>
        <Text>{'XXXX'}</Text>*/}
      </View>
      {/* */}
      <View />
      {/* */}
      <View />
      {/* */}
      <View />
      {/* */}
      <View />
      {/* */}
      <View />
    </Layout>
  )
}

export default BuildDetail
